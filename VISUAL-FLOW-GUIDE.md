# 🎯 VISUAL FLOW DIAGRAMS

## 📱 COMPLETE REGISTRATION FLOW

```
┌──────────────────────────────────────────────────────────────────┐
│                     USER VISITS WEBSITE                          │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│                    HOME PAGE (HomePage.jsx)                      │
│  • Hero section with CTA                                        │
│  • "Get Started" button                                         │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Click "Register"
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│               REGISTER PAGE (RegisterPage.jsx)                   │
│  • Full Name input                                              │
│  • Country Code dropdown (+91, +1, etc.)                        │
│  • Phone Number input (10 digits for India)                     │
│  • Email input                                                  │
│  • Password input (min 6 chars)                                 │
│  • "Create Account" button                                      │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Submit Form
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│              BACKEND: CHECK DUPLICATES (Back4App)                │
│  Query: Check if email exists                                   │
│  Query: Check if phone exists                                   │
└────────────────────────┬─────────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼                               ▼
    ┌─────────┐                   ┌──────────┐
    │ EXISTS? │                   │ NEW USER │
    │   YES   │                   │          │
    └────┬────┘                   └─────┬────┘
         │                              │
         ▼                              ▼
┌──────────────────────────┐   ┌────────────────────────────┐
│  SHOW ERROR MESSAGE      │   │  SEND OTP (Cloud Function) │
│  "Already registered"    │   │  • Generate 6-digit OTP    │
│  → Link to Login         │   │  • Save to database        │
│  → Link to Reset Pass    │   │  • Expires in 10 min       │
└──────────────────────────┘   │  • Send via SMS            │
                               └─────────┬──────────────────┘
                                         │
                                         ▼
┌──────────────────────────────────────────────────────────────────┐
│          OTP VERIFICATION PAGE (OTPVerificationPage.jsx)         │
│  • 6 input boxes for OTP digits                                 │
│  • Auto-focus on next box                                       │
│  • Copy-paste support                                           │
│  • 2-minute countdown timer                                     │
│  • "Resend OTP" button (after timer ends)                       │
│  • "Verify OTP" button                                          │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Submit OTP
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│            BACKEND: VERIFY OTP (Back4App)                        │
│  • Check OTP matches                                            │
│  • Check not expired                                            │
│  • Check not already verified                                   │
│  • Max 5 attempts                                               │
└────────────────────────┬─────────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼                               ▼
    ┌─────────┐                   ┌──────────┐
    │ INVALID │                   │  VALID   │
    │   OTP   │                   │   OTP    │
    └────┬────┘                   └─────┬────┘
         │                              │
         ▼                              ▼
┌──────────────────────────┐   ┌────────────────────────────┐
│  SHOW ERROR              │   │  CREATE USER ACCOUNT       │
│  "Invalid OTP"           │   │  • User table entry        │
│  (increment attempts)    │   │  • UserProfile entry       │
│                          │   │  • Mark as verified        │
└──────────────────────────┘   │  • Auto-login              │
                               └─────────┬──────────────────┘
                                         │
                                         ▼
┌──────────────────────────────────────────────────────────────────┐
│                 DASHBOARD (DashboardPage.jsx)                    │
│  • Welcome message                                              │
│  • Navigation menu                                              │
│  • Profile completion prompt (if incomplete)                    │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔑 LOGIN FLOW

```
┌──────────────────────────────────────────────────────────────────┐
│                  LOGIN PAGE (LoginPage.jsx)                      │
│  • Email OR Phone input                                         │
│  • Password input (with show/hide)                              │
│  • "Remember me" checkbox                                       │
│  • "Forgot Password?" link                                      │
│  • "Login" button                                               │
│  • Social login options (Google, Facebook)                      │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Submit
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│              BACKEND: AUTHENTICATE (Back4App)                    │
│  • Determine if input is email or phone                         │
│  • Query User table                                             │
│  • Validate password                                            │
└────────────────────────┬─────────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼                               ▼
    ┌─────────┐                   ┌──────────┐
    │ INVALID │                   │  VALID   │
    │  CREDS  │                   │  CREDS   │
    └────┬────┘                   └─────┬────┘
         │                              │
         ▼                              ▼
┌──────────────────────────┐   ┌────────────────────────────┐
│  SHOW ERROR              │   │  LOAD USER PROFILE         │
│  "Invalid credentials"   │   │  • Get UserProfile         │
│  (stay on login page)    │   │  • Create session          │
│                          │   │  • Store in localStorage   │
└──────────────────────────┘   │  • Redirect to Dashboard   │
                               └────────────────────────────┘
```

---

## 🔐 FORGOT PASSWORD FLOW

```
┌──────────────────────────────────────────────────────────────────┐
│          FORGOT PASSWORD PAGE (ForgotPasswordPage.jsx)           │
│                        STEP 1: Enter Details                     │
│  • Email OR Phone input                                         │
│  • "Send OTP" button                                            │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Submit
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│              BACKEND: SEND RESET OTP (Back4App)                  │
│  • Check if user exists                                         │
│  • Generate 6-digit OTP                                         │
│  • Save to database (type: password_reset)                      │
│  • Send via SMS/Email                                           │
└────────────────────────┬─────────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼                               ▼
    ┌─────────┐                   ┌──────────┐
    │  USER   │                   │   USER   │
    │NOT FOUND│                   │  FOUND   │
    └────┬────┘                   └─────┬────┘
         │                              │
         ▼                              ▼
┌──────────────────────────┐   ┌────────────────────────────┐
│  SHOW ERROR              │   │  GO TO STEP 2              │
│  "No account found"      │   │  • Show OTP input page     │
└──────────────────────────┘   │  • Start 2-min timer       │
                               └─────────┬──────────────────┘
                                         │
                                         ▼
┌──────────────────────────────────────────────────────────────────┐
│          FORGOT PASSWORD PAGE (ForgotPasswordPage.jsx)           │
│                        STEP 2: Enter OTP                         │
│  • 6 OTP input boxes                                            │
│  • 2-minute countdown                                           │
│  • "Verify OTP" button                                          │
│  • "Resend OTP" link                                            │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Submit OTP
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│              BACKEND: VERIFY OTP (Back4App)                      │
│  • Check OTP matches                                            │
│  • Check type = password_reset                                  │
│  • Check not expired                                            │
└────────────────────────┬─────────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼                               ▼
    ┌─────────┐                   ┌──────────┐
    │ INVALID │                   │  VALID   │
    │   OTP   │                   │   OTP    │
    └────┬────┘                   └─────┬────┘
         │                              │
         ▼                              ▼
┌──────────────────────────┐   ┌────────────────────────────┐
│  SHOW ERROR              │   │  GO TO STEP 3              │
│  "Invalid OTP"           │   │  • Show password fields    │
└──────────────────────────┘   └─────────┬──────────────────┘
                                         │
                                         ▼
┌──────────────────────────────────────────────────────────────────┐
│          FORGOT PASSWORD PAGE (ForgotPasswordPage.jsx)           │
│                     STEP 3: Set New Password                     │
│  • New Password input                                           │
│  • Confirm Password input                                       │
│  • "Reset Password" button                                      │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Submit
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│            BACKEND: UPDATE PASSWORD (Back4App)                   │
│  • Find user by email/phone                                     │
│  • Update password                                              │
│  • Hash automatically                                           │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│                    SUCCESS MESSAGE                               │
│  "Password reset successful!"                                   │
│  → Auto-redirect to Login (2 seconds)                           │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 OTP RESEND LOGIC

```
┌──────────────────────────────────────────────────────────────────┐
│               OTP PAGE (Timer running: 2:00)                     │
│  • User can enter OTP                                           │
│  • "Resend" button is DISABLED                                  │
│  • Timer counts down: 1:59, 1:58, 1:57...                       │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Time passes...
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│               OTP PAGE (Timer: 0:00)                             │
│  • Timer expired                                                │
│  • "Resend OTP" button is now ENABLED                           │
│  • User can click to resend                                     │
└────────────────────────┬─────────────────────────────────────────┘
                         │ Click "Resend OTP"
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│              BACKEND: SEND NEW OTP (Back4App)                    │
│  • Generate new 6-digit OTP                                     │
│  • Invalidate old OTP                                           │
│  • Save new OTP to database                                     │
│  • Send via SMS/Email                                           │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│               OTP PAGE (Timer reset: 2:00)                       │
│  • Clear all OTP inputs                                         │
│  • Reset timer to 2 minutes                                     │
│  • Disable "Resend" button again                                │
│  • Show success message "New OTP sent!"                         │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ DATABASE OPERATIONS

### Registration Flow:

```
1. DUPLICATE CHECK:
   ┌─────────────────────────────────┐
   │ Query: _User table              │
   │ WHERE email = user_email        │
   │ OR username = user_phone        │
   └─────────────────────────────────┘
         │
         ▼
   Result: User exists? → Error
   Result: No user? → Continue

2. SAVE OTP:
   ┌─────────────────────────────────┐
   │ INSERT INTO OTPVerification     │
   │ {                               │
   │   phone: "+919876543210"        │
   │   otp: "123456"                 │
   │   type: "registration"          │
   │   expiresAt: Date + 10min       │
   │   verified: false               │
   │   attempts: 0                   │
   │ }                               │
   └─────────────────────────────────┘

3. VERIFY OTP:
   ┌─────────────────────────────────┐
   │ Query: OTPVerification          │
   │ WHERE phone = user_phone        │
   │ AND otp = entered_otp           │
   │ AND verified = false            │
   │ AND expiresAt > NOW()           │
   └─────────────────────────────────┘
         │
         ▼
   Found? → Mark verified = true

4. CREATE USER:
   ┌─────────────────────────────────┐
   │ INSERT INTO _User               │
   │ {                               │
   │   username: "+919876543210"     │
   │   email: "user@example.com"     │
   │   password: "hashed_password"   │
   │ }                               │
   └─────────────────────────────────┘

5. CREATE PROFILE:
   ┌─────────────────────────────────┐
   │ INSERT INTO UserProfile         │
   │ {                               │
   │   userId: pointer_to_user       │
   │   fullName: "John Doe"          │
   │   phone: "+919876543210"        │
   │   email: "user@example.com"     │
   │   isVerified: true              │
   │   createdAt: NOW()              │
   │ }                               │
   └─────────────────────────────────┘
```

---

## 🔐 SECURITY CHECKPOINTS

```
┌──────────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                               │
└──────────────────────────────────────────────────────────────────┘

Layer 1: INPUT VALIDATION (Frontend)
├─ Email format validation
├─ Phone number format validation  
├─ Password length check (min 6)
├─ Empty field checks
└─ Character restrictions

Layer 2: DUPLICATE PREVENTION (Backend)
├─ Check email uniqueness
├─ Check phone uniqueness
└─ Show helpful error messages

Layer 3: OTP SECURITY (Backend)
├─ 10-minute expiration
├─ One-time use (marked as verified)
├─ Max 5 attempts per OTP
├─ Rate limiting on resend (2-minute cooldown)
└─ Cannot reuse expired OTPs

Layer 4: PASSWORD SECURITY (Parse)
├─ Automatic hashing (bcrypt)
├─ Never stored in plain text
├─ Cannot be retrieved (only reset)
└─ Secure comparison during login

Layer 5: SESSION SECURITY (Frontend + Backend)
├─ Token-based authentication
├─ LocalStorage for persistence
├─ Auto-logout on invalid token
├─ Protected routes
└─ Logout clears all session data

Layer 6: DATABASE SECURITY (Back4App)
├─ ACL (Access Control Lists)
├─ Master key for privileged operations
├─ User can only access own data
├─ No direct database access from frontend
└─ All operations through cloud functions
```

---

## 📊 USER STATES

```
┌──────────────────────────────────────────────────────────────────┐
│                    USER LIFECYCLE STATES                         │
└──────────────────────────────────────────────────────────────────┘

State 1: NEW VISITOR
├─ No account
├─ Can browse home page
├─ Can access register/login
└─ Cannot access dashboard

State 2: REGISTERING
├─ Filled registration form
├─ Waiting for OTP
├─ OTP sent to phone
└─ Account not yet created

State 3: VERIFYING OTP
├─ Entered OTP
├─ System checking validity
├─ Up to 5 attempts allowed
└─ Can request new OTP after 2 min

State 4: REGISTERED (Not Verified)
├─ Account exists
├─ But OTP not verified
├─ Cannot login yet
└─ Must complete verification

State 5: VERIFIED USER
├─ Account created
├─ OTP verified
├─ Can login anytime
└─ Profile may be incomplete

State 6: LOGGED IN
├─ Active session
├─ Can access dashboard
├─ Can complete profile
└─ Can browse matches

State 7: PROFILE COMPLETE
├─ All fields filled
├─ Photos uploaded
├─ Ready for matching
└─ Full feature access
```

---

## 🎯 ERROR HANDLING

```
┌──────────────────────────────────────────────────────────────────┐
│                    ERROR SCENARIOS                               │
└──────────────────────────────────────────────────────────────────┘

ERROR: Email Already Exists
├─ Detection: During registration
├─ Message: "This email is already registered"
├─ Action: Show link to login
└─ Action: Show link to forgot password

ERROR: Phone Already Exists
├─ Detection: During registration
├─ Message: "This phone number is already registered"
├─ Action: Show link to login
└─ Action: Show link to forgot password

ERROR: Invalid OTP
├─ Detection: During OTP verification
├─ Message: "Invalid or expired OTP"
├─ Action: Allow retry (up to 5 times)
└─ Action: Show resend option

ERROR: OTP Expired
├─ Detection: After 10 minutes
├─ Message: "OTP has expired"
├─ Action: Request new OTP
└─ Action: Reset timer to 2 minutes

ERROR: Invalid Login
├─ Detection: During login attempt
├─ Message: "Invalid email/phone or password"
├─ Action: Stay on login page
└─ Action: Offer forgot password link

ERROR: Network Failure
├─ Detection: API call fails
├─ Message: "Connection error. Please try again."
├─ Action: Retry button
└─ Action: Check internet connection

ERROR: SMS Send Failure
├─ Detection: SMS gateway error
├─ Message: "Failed to send OTP. Please try again."
├─ Action: Retry send OTP
└─ Action: Log error for debugging

ERROR: User Not Found (Forgot Password)
├─ Detection: During password reset
├─ Message: "No account found with this email/phone"
├─ Action: Show register link
└─ Action: Verify input format
```

---

## ✅ SUCCESS FLOWS

```
┌──────────────────────────────────────────────────────────────────┐
│                    SUCCESS SCENARIOS                             │
└──────────────────────────────────────────────────────────────────┘

SUCCESS: Registration Complete
├─ OTP verified
├─ Account created
├─ Profile created
├─ Auto-login
└─ → Navigate to Dashboard

SUCCESS: Login
├─ Credentials valid
├─ Session created
├─ User profile loaded
└─ → Navigate to Dashboard

SUCCESS: Password Reset
├─ OTP verified
├─ New password set
├─ Password updated in database
└─ → Navigate to Login (with success message)

SUCCESS: OTP Resent
├─ New OTP generated
├─ SMS/Email sent
├─ Timer reset
└─ → Show "New OTP sent!" message
```

This visual guide shows you EXACTLY how everything flows together! 🎉
